services:
  scheduler-web:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: "postgresql://readonly:change-me@postgres.example.com:5432/minigames?sslmode=require"
      DB_MAX_CONNECTIONS: "10"
      MC_STATUS_ADDRESS: "frp-hen.com:25568"
